<Window
    x:Class="YouHaveIssues.WPF.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:YouHaveIssues.WPF"
    mc:Ignorable="d"
    Title="You Have Issues"
    Height="350"
    Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="auto" />
            <RowDefinition
                Height="auto" />
            <RowDefinition
                Height="auto" />
            <RowDefinition
                Height="*" />
        </Grid.RowDefinitions>
        <Grid
            Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition
                    Width="auto" />
                <ColumnDefinition
                    Width="*" />
            </Grid.ColumnDefinitions>
            <Label
                Content="Auth token:"
                Grid.Column="0" />

            <TextBox
                x:Name="token"
                Grid.Column="1" />
        </Grid>
        <Button
            Content="Authenticate"
            Click="OnAuthenticateClicked"
            Grid.Row="1" />
        <ComboBox
            x:Name="repositoriesCombo"
            SelectionChanged="repositoriesCombo_SelectionChanged"
            Grid.Row="2" />
        <DataGrid
            x:Name="dataGrid"
            Grid.Row="3"/>
    </Grid>
</Window>
